@if (User.Identity.IsAuthenticated)
{
    <div class="row">
        <div class="col-md-12">
            <h1>Hello @User.Claims.FirstOrDefault(c => c.Type == "FullName")?.Value!</h1>
            <p>Username: @User.Identity.Name</p>
        </div>
    </div>
}

<h1>Cookie Sample</h1>
